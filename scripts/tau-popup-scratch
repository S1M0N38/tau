#!/usr/bin/env bash
set -euo pipefail

# tau-popup-scratch — toggle scratch shell popup (Cmd+S)

source "${TAU_ROOT}/lib/common.sh"

"$TAU_ROOT/scripts/tau-toggle-popup" scratch "${SHELL:-/bin/bash}" 90% 90%
